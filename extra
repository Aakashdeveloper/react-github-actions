  # Copy the files from build folder to the S3 bucket
      # - name: Deploy to S3
      #   zip -r reactaction.zip app.js Dockerfile dockerignore package.json
      #     S3_KEY="$AWS_VERSION_LABEL.zip"
      #   run:
      #   zip -r reactaction.zip app.js Dockerfile dockerignore package.json
      #   aws s3 sync . s3://react-github-actions-aakash --acl public-read
      #   working-directory: react-github-actions-build